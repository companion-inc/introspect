# Verification Matrix

| Claim | Deterministic check | Live smoke check | Evidence | Status | Follow-up |
| --- | --- | --- | --- | --- | --- |
| Scanner is not a timer/poller | Inspect installed plist for `StartInterval` absence and `WatchPaths` presence | `launchctl print` shows event trigger and not-running idle state | `~/Library/LaunchAgents/ai.companion.introspect.codex-scanner.plist:42-52`; launchctl output in this run | Pass | Add a regression check to `introspect-status.sh` that reports timer keys explicitly. |
| Health monitor is login repair, not a poller | Inspect installed health plist for `RunAtLoad` and no timer key | Open app or log in and confirm one run | `~/Library/LaunchAgents/ai.companion.introspect.health.plist:25-30` | Pass | Add explicit "no timer" wording to status UI. |
| One prompt can duplicate through hook and scanner | Compare `feedback/events.jsonl` source records for the current prompt | Trigger one Codex prompt and inspect queue | `feedback/events.jsonl:4714-4717` | Pass | Implement canonical event id and assert one queued event. |
| Worker lock prevents simultaneous reflectors | Watch duplicate starts during current prompt | Trigger hook and scanner close together | `feedback/reflector.log:12782-12789` | Pass | Lock works, but dedupe must happen before wake/queue noise. |
| Global cooldown/retry can interrupt unrelated work | Inspect worker state and queue while another project triggers | Keep active work open while Companion trigger fires | `hooks/trigger-worker.py:975-1015`; live queue with Companion events during this run | Pass | Add per-project/session isolation and quiet mode. |
| Classifier wakes are not final classifications | Check reflector result for current request | Confirm current request became `no_change` | `feedback/reflector.log:12790-12805` | Pass | Preserve no-change decisions in UI as useful signal. |
| Corpus is too large for manual transcript review | Run deduped transcript scan | N/A | Deduped command output: 35,654 user turns, 4,760 trigger turns | Pass | Build an index rather than ad hoc grep. |
| Biggest problem classes are routing failures, not profanity | Run category scan over deduped triggers | Sample representative transcripts | Deduped command output: top categories include question/confusion, constraints, context/docs, tech routing, live verification | Pass | Replace heuristic labels with evidence-bundle classifier. |
| Skills should be updated before duplicated | Validate source-map and skill-creator guidance | Trigger a known skill category and inspect proposed target | `skills/skill-creator/SKILL.md:18-20`; `skills/skill-creator/references/source-map.md:136-154` | Pass as design | Needs implementation test. |
| Prompt/skill writes should stage by default | Read Hermes and skill-manager reviews | Attempt proposal-only flow in UI | `docs/hermes-self-evolution-review.md:33-35`; `docs/skill-manager-reference-review.md:29` | Design pass | Pending UI implementation. |
| Auto-apply can mutate prompts from noisy aggregate trigger-rate reasoning | Inspect live reflector log and git commit | Observe background reflector during unrelated deep research | `feedback/reflector.log` tail; `git show be6bb08 -- AGENTS.md` | Pass, live failure observed | Make staging/approval the default before further auto prompt/skill writes. |
| Project-specific tech complaints route to project surfaces | Compare cwd/project with skill index before target choice | Use React state complaint from Companion | Deduped category scan; `README.md:91-96` | Not run | Needs classifier probe. |
| Real live-state failures require runtime verification | Category scan finds live verification failures | Replay one Companion staging failure | Deduped category scan; existing AGENTS guidance | Not run | Pick one historical Companion trigger and verify current code path separately. |
