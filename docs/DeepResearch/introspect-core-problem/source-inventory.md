# Source Inventory

| Source | Category | Evidence | Why it matters | Trust | Status |
| --- | --- | --- | --- | --- | --- |
| `README.md:3`, `README.md:11` | Local repo doc | Introspect is an app/hook engine for improving local Claude/Codex instructions from trigger signals; private Introspect home state belongs under `~/.introspect`. | Establishes product boundary between reusable engine and private Introspect home. | Primary | Opened |
| `README.md:24-36` | Local repo doc | App can apply prompt links, install hooks/scanner/health monitor, initialize home/project surfaces, edit trigger words, and show runs/diffs/status. | Defines current product surface and missing workflow continuity. | Primary | Opened |
| `README.md:51-59` | Local repo doc | Scanner is documented as event-driven on Codex writes; immediate mode enqueues and kicks one locked worker. | Separates "why did it wake" from "why does it need a backstop." | Primary | Opened |
| `README.md:72-83` | Local repo doc | Current flow logs prompt metadata, matches exact trigger words, queues events, scans missed Codex transcripts, batches under a worker lock, and selects one target. | Current pipeline being evaluated. | Primary | Opened |
| `README.md:87-110` | Local repo doc | Agent memory is a routing problem, not one giant prompt; learning layers include system prompt, project prompt, home memory, skills, and later training/adapters. | Confirms the user's stated product direction already matches repo intent. | Primary | Opened |
| `hooks/trigger-reflect.sh:5-16` | Local hook code | Hook is deliberately broad recall and only logs/queues; judgment happens out of band in `trigger-worker.py`. | Trigger words are input signals, not final classifications. | Primary | Opened |
| `hooks/trigger-reflect.sh:50-88`, `hooks/trigger-reflect.sh:120-181` | Local hook code | Matching is exact lowercase word matching; triggered prompts are queued and immediate mode starts one worker process. | Explains why emotional language can trigger the system and why duplicate starts happen. | Primary | Opened |
| `hooks/trigger-worker.py:43-47`, `hooks/trigger-worker.py:958-1015` | Local worker code | Defaults include 75 second debounce, 300 second global cooldown, 900 second session cooldown, and requeue/scheduled retry behavior. | Explains why background reflection can keep retrying after unrelated triggers. | Primary | Opened |
| `scripts/install-hooks.sh:405-449` | Local installer code | Scanner LaunchAgent is generated with `WatchPaths` on Codex history and sessions plus `RunAtLoad`; comments say this catches Desktop hooks that are skipped. | Source-level answer to wake/backstop question. | Primary | Opened |
| `scripts/install-hooks.sh:462-495` | Local installer code | Health monitor LaunchAgent uses `RunAtLoad` and no polling timer. | Distinguishes login repair from ongoing poll behavior. | Primary | Opened |
| `skills/skill-creator/SKILL.md:10-12` | Project skill | A skill is scoped procedural memory; create/update only for reusable lessons; casual profanity or external-system triggers should make no change. | Intended rule for turning emotion into agent memory without bloat. | Primary | Opened |
| `skills/skill-creator/SKILL.md:60-66` | Project skill | Decision rules include `skill_new`, `skill_update`, `skill_prune`, `core_prompt`, and `no_change`. | Provides operation vocabulary for the curator. | Primary | Opened |
| `skills/skill-creator/references/source-map.md:136-168` | Project reference | Reusable lessons should accumulate in skills, failed skills should be updated, compactness matters, and keyword-first routing should only upgrade when noisy. | Supports skill routing and pruning as first-class, not append-only. | Primary summary | Opened |
| `skills/skill-creator/references/source-map.md:174-181` | Project reference | Placement matrix maps false positives to `no_change`, always-on invariants to `core_prompt`, reusable workflows to skills, and must-happen requirements to hooks/scripts. | Routing table Introspect needs to implement with evidence. | Primary summary | Opened |
| `docs/hermes-self-evolution-review.md:22-61` | Local research doc | Hermes is useful as a layered memory/skills/curator reference, but too aggressive for trigger-driven writes; Introspect should prefer `no_change`, staging, and one decision per batch. | Validates product direction and cautions against eager skill writing. | Primary repo research | Opened |
| `docs/skill-manager-reference-review.md:14-30` | Local research doc | Skills need inventory UI, provider-aware scanning, duplicate audit, and staged proposals before autonomous writes. | Informs UI/control surface for making changes understandable and reversible. | Primary repo research | Opened |
| `/Users/advaitpaliwal/Library/LaunchAgents/ai.companion.introspect.codex-scanner.plist:42-52` | Installed LaunchAgent | Scanner has `RunAtLoad` and `WatchPaths` for `~/.codex/history.jsonl` and `~/.codex/sessions`; no `StartInterval`. | Refutes literal polling interval while proving event-driven wake sources. | Installed runtime | Opened |
| `/Users/advaitpaliwal/Library/LaunchAgents/ai.companion.introspect.health.plist:25-30` | Installed LaunchAgent | Health monitor has `RunAtLoad` and log paths; no timer key. | Shows health monitor is login repair, not continuous polling. | Installed runtime | Opened |
| `launchctl print gui/$(id -u)/ai.companion.introspect.codex-scanner` | Runtime command | Scanner state was `not running`, had FSEvents `WatchPaths` event trigger, and had `runs = 6` when checked. | Scanner wakes and exits; it is not a resident loop. | Local command output | Opened |
| `feedback/codex-scanner.out.log:464-503` | Runtime log | Repeated scanner runs often processed zero new events; recent lines also show repeated triggered queues. | User-visible noise can happen without a timer because watched files change frequently. | Local runtime log | Opened |
| `feedback/events.jsonl:4714-4717` | Feedback log | One current user request produced duplicate foreground hook and Codex scanner trigger records. | Direct evidence for repeated wake/noise as duplicate event paths, not a timer. | Local runtime log | Opened |
| `feedback/reflector.log:12782-12805` | Reflector log | Duplicate worker starts exited under lock; reflector classified the current request as `no_change` because it was product-domain venting, not an agent-behavior failure. | Confirms lock behavior and correct classification target. | Local runtime log | Opened |
| `feedback/reflector.log` tail after 2026-06-14T00:15:16Z | Reflector log | A later retry classified a background-task wait as `core_prompt`, reverted `AGENTS.md`, and reported commit `be6bb08`. | Strong evidence that auto-apply plus noisy aggregate trigger-rate reasoning is unsafe. | Local runtime log | Opened |
| `git show --format=fuller --stat --patch be6bb08 -- AGENTS.md` | Git command output | Commit `be6bb08` deleted the explicit-constraints AGENTS rule and used trigger-rate spike reasoning in its commit message. | Confirms the reflector performed a real prompt mutation and commit during this research pass. | Local git history | Opened |
| `feedback/health-status.latest:1-30` | Status output | Prompt links, hooks, scanner, health monitor, notifications setting, skill validation, event counts, queue status, and scanner status were visible. | Confirms live installation is active and broad enough to need summarization. | Local status file | Opened |
| Raw transcript scan command | Local machine evidence | Raw records spanned 2025-09-07 to 2026-06-14; first pass counted 61,685 Codex user-message records and 7,523 Claude user-message records, but Codex duplicates made this an overcount. | Shows scale and why final numbers use dedupe. | Local command output | Opened |
| Deduped transcript scan command | Local machine evidence | Deduped records spanned 2025-09-07 to 2026-06-14; Codex had 31,329 user turns and 3,874 trigger turns; Claude had 4,325 user turns and 886 trigger turns. | Establishes corpus scale and trigger density. | Local command output; deduped by tool/path/text hash | Opened |
| Deduped trigger-category scan command | Local machine evidence | Category counts were question/confusion mode errors 1,802; explicit constraints ignored 1,579; missing prior context/docs 1,183; tech-specific routing 979; verify real/live state 724; scope/background-work sprawl 427; continue/resume pressure 292. | Shows the core problem is routing repeated failure shapes into durable memory, not profanity detection. | Local command output; heuristic discovery labels | Opened |
| `/Users/advaitpaliwal/.codex/memories/MEMORY.md:203-206` | Local memory registry | Prior user preference says skill/MCP/plugin decisions should be ranked by actual usage history and context value, not names alone. | Supports evidence-based routing and usage-backed skill pruning. | Local memory, possibly stale | Opened |
