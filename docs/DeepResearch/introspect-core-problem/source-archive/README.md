# Source Archive

No bulky raw transcripts are copied here.

Raw source locations used in this research:

- Codex sessions: `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`
- Claude sessions: `~/.claude/projects/<encoded-cwd>/*.jsonl`
- Introspect feedback: `feedback/events.jsonl`, `feedback/reflector.log`, `feedback/codex-scanner.out.log`
- Installed LaunchAgents: `~/Library/LaunchAgents/ai.companion.introspect.codex-scanner.plist`, `~/Library/LaunchAgents/ai.companion.introspect.health.plist`

The transcript scans were run as local one-off Python commands and summarized in `source-inventory.md`. They should be promoted to a checked-in script before being used as a repeatable product metric.
